[workspace]
members = [
    "common",
    "installer",
    "installer_builder",
    "uninstaller",
    "winui_support",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
opt-level = "z"
panic = "abort"
